[
  {
    "dashboard_url": "<%= p('broker.dashboard_uri')%>",
    "collector_ips": [<% link('collector_link').instances.each_with_index do | ip, index| %>"<%= link("collector_link").instances[index].address %>"<%= ',' if index < (link('collector_link').instances.size - 1) %><% end %>],
    "collector_span_port": "<%= link('collector_link').p('broker.collector_span_port')%>",
    "collector_stat_port": "<%= link('collector_link').p('broker.collector_stat_port')%>",
    "collector_tcp_port": "<%= link('collector_link').p('broker.collector_tcp_port')%>"
  }
]
